[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "minions-content-research"
version = "0.2.0"
description = "Research sources, topic clusters, keyword sets, and content briefs"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
keywords = ["content-research","ai","minions"]
authors = [
    { name = "Mehdi Nabhani", email = "mehdi@the-mehdi.com" }
]
dependencies = [
    "minions-sdk>=0.2.1",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[project.urls]
Homepage = "https://github.com/mxn2020/minions-content-research"
Repository = "https://github.com/mxn2020/minions-content-research"

[tool.hatch.build.targets.wheel]
packages = ["minions_content_research"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
